---
description: Show opencode-antigravity-auth Google/Antigravity quota buckets
---
Run this exact command:

```bash
npx --yes --package @zeklop/opencode-antigravity-auth@latest opencode-antigravity-quota
```

Then show the command output to the user as-is. Do not summarize it, do not hide reset times, and do not call any other tools unless the command fails.
